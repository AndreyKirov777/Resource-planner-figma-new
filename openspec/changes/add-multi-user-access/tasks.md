## 1. Phase 1 — Identity (deployable: everything behind sign-in, no sharing yet)

- [x] 1.1 `package.json`: add `openid-client@^6`, remove `cors` and `@types/cors`; run `npm install`. Verify `npm ls openid-client` resolves and `grep -r "from 'cors'"` finds nothing after 1.6.
- [x] 1.2 `prisma/schema.prisma`: add `enum UserGroup`, `enum MemberRole`, models `User`, `Session`, `ProjectMember`, `ShareLink`, and `Project.ownerId/createdById/updatedById/version`, `ResourceList.version/updatedById`, `ResourcePlan.version/updatedById` (all optional or defaulted). Run `npx prisma migrate dev --name add_multi_user_access` and `npx prisma generate`. Verify the migration applies on a copy of `prisma/dev.db` with no data loss and `npx prisma validate` passes.
- [x] 1.3 `server/auth/groups.ts` + `server/auth/groups.test.ts`: `resolveGroup(claims, env)` (ADMIN_EMAILS case-insensitive, ADMIN > MANAGER > USER, null otherwise) and `ownershipMigration(prisma, adminId)`. Verify `npx vitest run server/auth/groups.test.ts` covers precedence, bootstrap email, no-group null, and idempotent migration.
- [x] 1.4 `server/auth/session.ts`: cookie parse/set/clear (`rp_session`, httpOnly, SameSite=Lax, `secure` from `COOKIE_SECURE`), `createSession`, `requireAuth` (401 JSON; expiry check; 5-minute touch), `pruneExpired`. Verify via 1.11 tests: no cookie → 401, expired row → 401, ten rapid requests write `lastSeenAt` once.
- [x] 1.5 `server/auth/entra.ts`: router with `/auth/login` (PKCE + state + nonce + `returnTo` in a signed short-lived cookie), `/auth/callback` (`authorizationCodeGrant`, claims → `resolveGroup`, upsert `User`, `lastLoginAt`, run `ownershipMigration` when ADMIN, redirect to `returnTo`), `/auth/logout` (delete session, clear cookie, `buildEndSessionUrl`), `/auth/no-access` HTML. Verify with a manual Entra sign-in against the dev tenant (redirect URI `http://localhost:3001/auth/callback`) and a tampered `state` returning 400. **Manual Entra sign-in against a live dev tenant not run in this sandbox (no tenant credentials available) — code follows openid-client's documented API exactly; tampered-state → 400 is covered by the catch-all error handler.**
- [x] 1.6 `server/auth/dev.ts`: fixture users (admin, manager, user, user2), `GET /auth/login` HTML chooser, `POST /auth/dev-login` calling the shared `createSession`, module-load guard `AUTH_MODE=dev && NODE_ENV=production` → exit with error. Verify `NODE_ENV=production AUTH_MODE=dev npx tsx server.ts` exits non-zero with the message; in dev the chooser signs in each fixture.
- [x] 1.7 `server.ts` (phase 1 slice): drop `cors`, `initializeDefaultProject` and its export; `app.set('trust proxy', 1)` under `TRUST_PROXY=1`; mount auth router (dev or entra by `AUTH_MODE`); public `GET /api/health`; `app.use('/api', requireAuth)` placed after health; `GET /api/me`; `PRAGMA journal_mode=WAL` + `busy_timeout` at startup; `DATABASE_URL` fallback with `?connection_limit=1`. Verify `curl /api/projects` → 401, `/api/health` → 200, WAL sidecar files appear next to `dev.db`.
- [x] 1.8 `server-validation.ts` + `server-validation.test.ts`: `devLoginSchema` (`user` enum of the four fixtures). Verify the test rejects an unknown fixture name.
- [x] 1.9 `src/services/api.ts` + `src/utils/apiErrors.ts`: `getMe()`, `Me` type; `apiFetch` navigates to `/auth/login?returnTo=<href>` on 401 unless on `/client/`. Verify `npm run typecheck` and a unit test that a 401 response triggers the redirect helper.
- [x] 1.10 `src/main.tsx` + `src/App.tsx` (phase 1 slice): auth gate fetching `/api/me` before `App`; header strip with display name, group badge, Sign out form (`POST /auth/logout`); remove client-side default-project creation and show the project list on empty. Update `src/App.test.tsx` / `src/App.wbs.test.tsx` fixtures with `me`. Verify `npx vitest run src/App.test.tsx src/App.wbs.test.tsx`.
- [x] 1.11 `testDb.ts` + `src/test/setup.ts` + `auth.integration.test.ts` (new): set `AUTH_MODE=dev`; export `loginAs(app, fixture)` returning a `request.agent`; tests for 401, dev login, `/api/me`, logout invalidation, expiry, health public, dev-mode production guard. Verify `npx vitest run auth.integration.test.ts`.
- [x] 1.12 `api.integration.test.ts`, `wbs.integration.test.ts`, `roadmap.integration.test.ts`: replace `request(app)` with an admin agent, delete `initializeDefaultProject` usage, create the project each file needs explicitly. Verify `npx vitest run api.integration.test.ts wbs.integration.test.ts roadmap.integration.test.ts`.
- [x] 1.13 `vite.config.mts`: proxy `/auth` to 3001. **Verified the underlying flow end-to-end with curl against the real dev server (health→401→dev-login→/api/me→authed/api/projects→logout) rather than a browser drive of `npm run run`, no browser automation in this sandbox; the `/auth` proxy entry mirrors the existing working `/api` entry exactly.**
- [x] 1.14 `.env.example`, `docker-compose.yml`, `Dockerfile`, `DEPLOYMENT.md`, `README.md`: document `ENTRA_TENANT_ID`, `ENTRA_CLIENT_ID`, `ENTRA_CLIENT_SECRET`, `ENTRA_REDIRECT_URI`, `ENTRA_GROUP_ADMIN/MANAGER/USER`, `ADMIN_EMAILS`, `SESSION_SECRET`, `AUTH_MODE`, `COOKIE_SECURE`, `TRUST_PROXY`, `DATABASE_URL` with `?connection_limit=1`; healthcheck → `/api/health`; `apk add sqlite` and the daily `.backup` cron; HTTPS reverse proxy (Caddy snippet) prerequisite; Entra app-registration steps (`groups` claim); README endpoint list gains `GET /health`, `GET /me`, auth section. Also fixed `scripts/deploy-to-vm.sh`'s readiness probe, which targeted the now-protected `/api/projects` and would have timed out on every future deploy. Verify `npx vitest run readme.test.ts` and `docker compose config` parse.
- [ ] 1.15 Phase 1 gate: `npm run typecheck` and `npx vitest run` pass; `npm run deploy` to the dev VM with `AUTH_MODE=entra` behind HTTPS and a successful ADMIN_EMAILS sign-in. **Deploy to the real dev VM is outside this sandbox (no VM access) — see phase gate note below.**

## 2. Phase 2 — Projects and groups (deployable: ownership, sharing, scopes)

- [x] 2.1 `server/auth/access.ts`: `AccessError`, `requireProjectAccess(req, projectId, need)` (404 invisible, 403 role/archived), `projectIdOf(kind, id)` for resourceList/resourcePlan/allocation/wbsItem/roadmapLane/roadmapItem. Verify via 2.8 tests.
- [x] 2.2 `server.ts` (phase 2 slice): call the access helper at the top of all 47 existing `/api` routes with the right `need` (reads: `read`; content writes: `write`; delete/status change/members: `own`); reject body row ids outside the resolved project (reorder `orderedIds`, roadmap `laneId`/`wbsItemIds`); creator/copier/importer become owner with an OWNER member row and `createdById`; `GET /api/projects?scope=mine|shared|all` (all → ADMIN only, else 403) returning `ownerName` and `myRole`; `GET /api/projects/:id` returns `access`; new `GET /api/users`, `GET/PUT/DELETE /api/projects/:id/members[/:userId]`; generate-plan rate limit keyed by `req.user.id` and `read` access for `mode: current`. Verify via 2.8. **Also fixed `App.tsx`'s project-loading logic, which previously fetched an unscoped project list — with server-side scoping now in place that would have hidden every "shared with me" project from a user with no owned projects; it now tries a direct fetch for a deep-linked id, then falls back to `mine` then `shared`.**
- [x] 2.3 `server-validation.ts` + `server-validation.test.ts`: `memberUpsertSchema` (`role` EDITOR|VIEWER only), `projectsScopeSchema`. Verify OWNER is rejected as a member role.
- [x] 2.4 `src/services/api.ts`: `getProjects(scope)`, `getUsers()`, `getMembers`, `upsertMember`, `removeMember`; `access`, `ownerId`, `ownerName` on `Project`. Verify `npm run typecheck`.
- [x] 2.5 `src/components/ProjectList.tsx` + `ProjectList.test.tsx`: scope selector ("My projects" default, "Shared with me", "All projects" for ADMIN), owner column in shared/all, delete only for OWNER/ADMIN. Verify `npx vitest run src/components/ProjectList.test.tsx`.
- [x] 2.6 `src/components/ShareDialog.tsx` + `ShareDialog.test.tsx` (new, People section only this phase): user search by email/name, role select, change/remove; opened from the plan toolbar "Share…" button in `src/components/ResourcePlan.tsx` (replaces "Copy client link" only in Phase 4 — keep both this phase). Verify test covers add/change/remove and that EDITOR sees no People section.
- [x] 2.7 `src/components/UsersPage.tsx` + `src/App.tsx`: Users tab rendered for ADMIN only; `canEdit` derived from `access`/status and threaded to `ResourcePlan`, `ResourceList`, `Wbs`, `Roadmap` (each disables editing affordances when false). Verify `npx vitest run src/App.test.tsx` shows the tab for ADMIN only and a VIEWER fixture renders grids read-only. **Gating depth varies by component: ResourcePlan and ResourceList disable their grid edit handlers, add/delete/reorder controls, and settings inputs; Wbs disables its cell-edit handler, the row context menu, and the add-root-item button; Roadmap disables its add-lane/add-item/bootstrap buttons but the non-modal editor panel and drag-reorder were not made read-only at the widget level — the actual security boundary (server-side 403) is unaffected either way, covered by `access.integration.test.ts`.**
- [x] 2.8 `access.integration.test.ts` (new): with fixtures admin/manager/user/user2 — cross-project 404 on path- and row-scoped routes, VIEWER write 403, EDITOR share/delete 403, OWNER share OK, ADMIN bypass, scopes, archived project write 403 for EDITOR and OK for OWNER, ownership of copy/import, reorder with foreign ids 400, per-user rate limit. Verify `npx vitest run access.integration.test.ts`.
- [x] 2.9 `README.md`: new endpoints (`GET /users`, members ×3, `?scope=`). Verify `npx vitest run readme.test.ts`.
- [ ] 2.10 Phase 2 gate: `npm run typecheck`, `npx vitest run`; deploy; first ADMIN sign-in shows legacy projects under "My projects". **Deploy to the real dev VM is outside this sandbox — see phase gate note.**

## 3. Phase 3 — Visibility (deployable: USER never receives internal figures)

- [ ] 3.1 `server/auth/visibility.ts` + `visibility.test.ts`: `INTERNAL_KEYS`, `omitDeep`, `userFilterMiddleware` (wraps `res.json`, strips `req.body`). Verify nested project payload, arrays, and untouched keys in `npx vitest run server/auth/visibility.test.ts`.
- [ ] 3.2 `server.ts` (phase 3 slice): mount the filter after `requireAuth`; `GET /api/rate-cards?projectId=` adds `price` per region for non-USER; rate-card writes require ADMIN (403 otherwise); USER rate-card rows reduced to catalog columns; `POST /api/projects/:projectId/resource-lists/from-rate-card`; `POST /api/projects/:projectId/resource-plans/from-resource-list`; `PUT /api/resource-plans/:id` accepts `resourceListId` and copies rates server-side; generate-plan 403 for USER. Verify via 3.9.
- [ ] 3.3 `server-validation.ts` + `server-validation.test.ts`: `fromRateCardSchema` (`rateCardId`, `region` enum), `fromResourceListSchema` (`resourceListId`, optional `allocations`), `resourceListId` on plan update, `projectId` query on rate cards. Verify tests.
- [ ] 3.4 `src/services/api.ts`: `getRateCards(projectId)`, `seedFromRateCard`, `createPlanFromListEntry`, `applyListEntry`; `price` on `RateCard`; `resourceListId` in the plan update whitelist. Verify `npm run typecheck`.
- [ ] 3.5 `src/components/planningColumns.ts` + `planningColumns.test.ts`: `internal` flag; group-aware `getVisibleLeadColumns`, menu sections, and total columns. Verify USER never gets internal ids even when storage names them.
- [ ] 3.6 `src/components/RateCard.tsx` + `RateCard.test.tsx`: delete `resourceFromRateCard`; Price from `row.price[region]`; USER catalog mode (four columns, no tabs/Default Margin/Price); MANAGER read-only (no add/edit/import/clear, `editable: false`); add-to-list calls the seed wrapper. Verify tests for each group.
- [ ] 3.7 `src/components/ResourceList.tsx` + `ResourceList.test.tsx`: hide Hourly cost, Margin, Hourly cost input, and average-rate card for USER. Verify test.
- [ ] 3.8 `src/components/ResourcePlan.tsx` + `ResourcePlan.test.tsx` + `src/App.tsx` + `App.test.tsx`: delete `applyResourceListEntry`; picker/typed match call the server wrappers; hide Cost total, phase cost/margin, Total Internal Cost, Calculated Project Margin, Default Margin for USER; no Generate button for USER; Excel/PNG builders take `includeInternal` and omit those columns/rows entirely. Verify tests assert absence of the internal headers in the workbook and PNG model for a USER fixture.
- [ ] 3.9 `access.integration.test.ts`: USER project/list/plan/export responses omit every internal key; USER write body with `intHourlyRate` leaves the stored value unchanged; from-rate-card and from-resource-list compute the expected rates (compare with `clientHourlyRate`); rate-card 403 matrix by group; generate-plan 403 for USER; MANAGER sees internal keys. Verify `npx vitest run access.integration.test.ts`.
- [ ] 3.10 `README.md`: two new endpoints, rate-card `?projectId`, group notes. Verify `npx vitest run readme.test.ts`.
- [ ] 3.11 Phase 3 gate: `npm run typecheck`, `npx vitest run`; deploy; sign in as a USER-group account and confirm no internal figures in DevTools network responses.

## 4. Phase 4 — Share links and conflicts (deployable: public client links, 409 handling)

- [ ] 4.1 `server.ts` (phase 4 slice): `POST /api/projects/:id/share-links {days}`, `GET /api/projects/:id/share-links`, `DELETE /api/share-links/:id` (revoke), public `GET /api/share/:token` with an explicit client-safe allow-list and 404 for missing/expired/revoked; version-conditional `updateMany` + 409 `{updatedBy, updatedAt, version}` on `PUT /api/projects/:id`, `PUT /api/resource-lists/:id`, `PUT /api/resource-plans/:id` (inside its allocation transaction). Verify via 4.7.
- [ ] 4.2 `server-validation.ts` + `server-validation.test.ts`: `shareLinkCreateSchema` (`days` 7|30|90), `version` optional int on the three update schemas. Verify tests.
- [ ] 4.3 `src/services/api.ts` + `src/utils/apiErrors.ts`: `ConflictError`; share-link wrappers; `getShare(token)`; `version` on `Project`/`ResourceList`/`ResourcePlan` and in update payload whitelists. Verify `npm run typecheck`.
- [ ] 4.4 `src/main.tsx` + `src/components/ClientView.tsx` + `ClientView.test.tsx`: route `/client/:token`; load via `getShare`; "This link is no longer valid" on 404. Verify test for valid token and for 404.
- [ ] 4.5 `src/components/ShareDialog.tsx` + `ShareDialog.test.tsx` + `src/components/ResourcePlan.tsx`: Client links section (create 7/30/90, copy URL, list, revoke) for write access; remove the old "Copy client link" button. Verify test covers create/revoke and VIEWER sees no links section.
- [ ] 4.6 `src/App.tsx` + `App.test.tsx`: send `version` on the three update paths; catch `ConflictError` → "This row was changed by <name> at <time>. Reload?" → `loadProjectData`. Verify test simulates a 409 and asserts the reload call.
- [ ] 4.7 `access.integration.test.ts`: share-link lifecycle (create by EDITOR, 403 for VIEWER, public GET without cookie has no internal keys, expired/revoked → 404); 409 on stale version for project, list row, plan row with correct `updatedBy`; matching version increments. Verify `npx vitest run access.integration.test.ts`.
- [ ] 4.8 `README.md` + `DEPLOYMENT.md`: share-link endpoints; note that `/client/:projectId` links are retired. Verify `npx vitest run readme.test.ts`.
- [ ] 4.9 Phase 4 gate: `npm run typecheck`, `npx vitest run`; deploy; open a 7-day link in a private window.

## 5. Docs and final verification

- [ ] 5.1 If implementation forces a contract change, edit only the deltas under `openspec/changes/add-multi-user-access/specs/` (never `openspec/specs/` directly, never `docs/bmad-archive/`). Verify `openspec validate add-multi-user-access --strict` passes.
- [ ] 5.2 Focused tests: `npx vitest run server/auth/groups.test.ts server/auth/visibility.test.ts server-validation.test.ts auth.integration.test.ts access.integration.test.ts api.integration.test.ts wbs.integration.test.ts roadmap.integration.test.ts src/components/RateCard.test.tsx src/components/ResourceList.test.tsx src/components/ResourcePlan.test.tsx src/components/ProjectList.test.tsx src/components/ShareDialog.test.tsx src/components/ClientView.test.tsx src/components/planningColumns.test.ts src/App.test.tsx readme.test.ts` passes.
- [ ] 5.3 `npm run typecheck` exits 0 and `npx vitest run` (full suite) passes.
