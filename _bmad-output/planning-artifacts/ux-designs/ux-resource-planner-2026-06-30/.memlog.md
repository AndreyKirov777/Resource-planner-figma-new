---
topic: AI Plan Generation UI (Goal 2) — Generate AI Plan surface in Resource Planner
goal: Distill DESIGN.md + EXPERIENCE.md for the Generate-AI-Plan UX
updated: 2026-06-30T18:31
---

- (decision by user) Working mode = Coaching path: walk key UX decisions together, weave creative tools (mockups) along the way.
- (decision by user) Deliverables = markdown spines + HTML key-screen mockups (form / loading / result / error / empty), styled to project shadcn tokens.
- (decision by user) Result-review surface = right-docked Sheet (sheet.tsx). Form+loading+result all live in ONE right Sheet; the ResourcePlan grid stays visible to the left and is NEVER mutated until/unless accepted. Chosen over modal-inline, fullscreen review, and in-grid preview. Roomy-but-safe; a clean step toward the future §11 overlay.
- (override by user) spec-2 frozen section names GeneratePlanModal + Dialog; user re-negotiated to a right Sheet (Radix-Dialog-based, so dialog semantics preserved). spec-2 Code Map/Tasks need Dialog->Sheet, GeneratePlanModal->GeneratePlanSheet update. Owner (Andrey) approved the override.
- (event by coach) Data-availability check on live /api/projects/generate-plan (generateResourcePlan.ts:46-63): response = { draft:{ resourcePlans[ role, clientRole:null, name:null, intHourlyRate, clientHourlyRate, displayOrder, rationale?, allocations[] ], phases? }, warnings[] }. teamShape + selectedDisciplines are computed for LLM reasoning but DROPPED before response — not available to UI without a backend change. NOTE: spec-2 api.ts GeneratePlanResourcePlan type omits rationale?; should add it to match the wire payload.
- (decision by user) Result-view richness = Rich within available data: warnings callout + table(role/int/client, 2-dp rates) + per-row rationale expander + per-row allocations sparkline + phases-summary when present. NO teamShape/disciplines chips (not in response payload; would need backend change). Implies api.ts GeneratePlanResourcePlan must add rationale?.
- (decision by user) Accept scope = Stub now + Goal-3 flow in spec: v1 Accept fires console.log('TODO Goal 3', draft), Sheet stays open (per spec-2). EXPERIENCE.md documents the Goal-3 Accept->apply (append/replace) + 'Plan applied — Undo' toast + History as a clearly-marked FUTURE flow, not built in v1.
- (decision by user) Communication language = English (user requested mid-session; overrides the casual Russian intake).
- (event by coach) Region label->value map locked from RateCard.tsx headers: Ukraine/Eastern Europe/Asia (GE)/Asia (ARM, KZ)/LATAM/Mexico/India/New York/London.
- (event by coach) Rendered .working/generate-ai-plan-mockups.html — 6 frames: toolbar entry, form(idle), loading, result(rich), error(recoverable), Goal-3 future apply. Styled to live globals.css tokens.
- (event by coach) Drafted DESIGN.md (delta spec: warning amber token set + ai-accent violet glyph + Sheet treatment + tabular-nums numeric cols) and EXPERIENCE.md (Foundation/IA/Voice/Components/States/Interactions/A11y/3 Key Flows incl. Goal-3 future). status=draft pending review.
- (event by user) FEEDBACK on Proposed-plan screen: need PRECISE allocation BY PHASE, granularity 2h/day (8h max => 5 levels 0/2/4/6/8h = 0/25/50/75/100%). Compact display, prefer bars with height encoding allocation.
- (event by coach) Grounded in code: allocation = per-period int 0-100 (grid shows %, heat-maps 0->white,100->#63BE7B). 8h/day, weekly=40h, monthly=daysInFTE*8. Phases group periods (PHASE_COLORS pastels). Rendered .working/allocation-display-variants.html: A=intensity gantt (width=duration,height=h/day), B=equal bars+labels, C=phase chips; +row expander w/ precise weeks/h-per-day/% table. Phase pastel = fill (which phase), height = amount (how much), no double-encode.
- (decision by user) Allocation snapping = snap to nearest 2h/day step (0/2/4/6/8h) for BOTH bar height and shown value. Plan reads in native 2h granularity; AI's raw % treated as approximate (e.g. 80% -> 75%/6h).
- (event by user) User rejected all 3 allocation-display variants (A gantt / B equal bars / C chips); will provide own vision for the allocation-by-phase visual in chat. PENDING.
- (decision by user) Allocation visual (final vision): keep the compact in-column monochrome bar chart (like reference screenshot). ONE BAR = ONE WEEK across the whole timeline, bar HEIGHT = allocation snapped to 0/25/50/75/100%. Above the bars, a phase header shows phase NAMES + WEEK COUNTS (band widths proportional to weeks); bars grouped by phase. Row expander gives precise per-phase weeks/h-day/% table (also the a11y text alt). Rendered .working/result-allocation-v2.html.
- (decision by user) Long-timeline handling = horizontal scroll: keep 1 bar per week; when weeks overflow the Allocation column, the per-week bar strip (and its phase header) scroll horizontally together. No per-phase collapse, no Sheet-width growth as the primary mechanism.
- (event by user) Per-week timeline approved 'close, small tweaks' — user to provide adjustments before wiring into spines. PENDING tweaks.
- (decision by user) Tweaks to per-week timeline: REMOVE the row 'Allocation detail' expander table and REMOVE the scale legend. Bars + phase header (names + week counts) only. a11y: since the visible expander is gone, each role's bar strip carries a non-visual text alternative (aria-label/title summarizing per-phase weeks + %).
- (decision by user) Phase header order swapped: week-count line (2w/8w/2w) on TOP, colored phase-name bands (Discovery/Build/Launch) on the BOTTOM sitting directly above the per-week bars.
- (event by coach) Wired per-week allocation timeline into the canonical gallery mockup (result frame): phase header (week-counts over pastel bands) + 1 bar/week snapped 0/25/50/75/100% + horizontal scroll. Updated DESIGN.md (alloc-bar tokens, alloc-timeline component, Do's) and EXPERIENCE.md (allocation pattern, phases prop, snapping, scroll, a11y aria-label, hint copy, state table, open items). Promoted mockups -> mockups/generate-ai-plan.html + mockups/proposed-plan-allocation.html.
- (event by coach) Consistency sweep: removed stale 'phase pills' refs (replaced by allocation phase bands, new rounded.band token); rationale expander retained (valid). Editorial self-polish done.
- (event) spines finalized
- (event by coach) Wrote 3 corrections back into spec-2-generate-plan-ui.md: (1) added rationale? to GeneratePlanResourcePlan type; (2) Dialog->Sheet, GeneratePlanModal->GeneratePlanSheet across Code Map/Tasks/Acceptance/Verification; (3) added phases+planningMode props. Frozen block (lines 10-48) left byte-intact; added a 'Post-approval amendments' note after it recording the owner-approved override + a result-view scope pointer to the UX spines.
